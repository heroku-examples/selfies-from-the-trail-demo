{
  "kafka": {
    "url": "$KAFKA_URL",
    "submissionTopic": "${KAFKA_PREFIX}submission-app"
  },
  "aws": {
    "id": "$BUCKETEER_AWS_ACCESS_KEY_ID",
    "secret": "$BUCKETEER_AWS_SECRET_ACCESS_KEY",
    "region": "$BUCKETEER_AWS_REGION",
    "bucket": "$BUCKETEER_BUCKET_NAME"
  },
  "client": {
    "tweet": {
      "text": "A message to tweet along with the url",
      "hashtags": "dreamforce,heroku",
      "via": "heroku",
      "related": "heroku,salesforce"
    }
  }
}
